#!/bin/bash

# I use this script instead of a cron job becouse of problems with cron and
# gnome-keyring. Add this to the users startup scripts/applications

while true; do
    /usr/bin/offlineimap -u quiet
    sleep 1m
done
